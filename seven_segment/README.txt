The following code contain  display digit 0-9 in seven segment display.
also display some of alphabet A,b,c,d,E etc.
following code output is observed in picimlab emulator.
the project multiplexed seven segment contaim increment decrement counter push button from 00-99,when both digit is  same on seven segment the buzzer sound.